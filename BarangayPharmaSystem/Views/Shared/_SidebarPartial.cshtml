@* 
   _SidebarPartial.cshtml — Role-based navigation sidebar.
   Renders different nav items depending on the authenticated user's role.
*@

@{
    var currentArea       = ViewContext.RouteData.Values["area"]?.ToString() ?? "";
    var currentController = ViewContext.RouteData.Values["controller"]?.ToString() ?? "";
    var currentAction     = ViewContext.RouteData.Values["action"]?.ToString() ?? "";

    string NavClass(string area, string controller, string? action = null)
    {
        bool areaMatch       = string.Equals(currentArea, area, StringComparison.OrdinalIgnoreCase);
        bool controllerMatch = string.Equals(currentController, controller, StringComparison.OrdinalIgnoreCase);
        bool actionMatch     = action == null || string.Equals(currentAction, action, StringComparison.OrdinalIgnoreCase);
        return (areaMatch && controllerMatch && actionMatch) ? "bps-nav-link active" : "bps-nav-link";
    }
}

@* ── ADMIN ONLY ───────────────────────────────────────────────────────────── *@
@if (User.IsInRole("Admin"))
{
    <div class="bps-nav-section">
        <span class="bps-nav-section-label">Overview</span>
        <a asp-area="Admin" asp-controller="Dashboard" asp-action="Index" class="@NavClass("Admin", "Dashboard")">
            <i class="bi bi-grid-1x2-fill bps-nav-icon"></i>
            <span>Dashboard</span>
        </a>
    </div>

    <div class="bps-nav-section">
        <span class="bps-nav-section-label">Administration</span>
        <a asp-area="Admin" asp-controller="Users" asp-action="Index" class="@NavClass("Admin", "Users")">
            <i class="bi bi-person-badge-fill bps-nav-icon"></i>
            <span>User Accounts</span>
        </a>
        <a asp-area="Admin" asp-controller="AuditLogs" asp-action="Index" class="@NavClass("Admin", "AuditLogs")">
            <i class="bi bi-shield-lock-fill bps-nav-icon"></i>
            <span>Audit Logs</span>
        </a>
    </div>

    <div class="bps-nav-section">
        <span class="bps-nav-section-label">Inventory & Vendor</span>
        <a asp-area="Admin" asp-controller="Medicines" asp-action="Index" class="@NavClass("Admin", "Medicines")">
            <i class="bi bi-capsule bps-nav-icon"></i>
            <span>Medicines</span>
        </a>
        <a asp-area="Admin" asp-controller="Suppliers" asp-action="Index" class="@NavClass("Admin", "Suppliers")">
            <i class="bi bi-truck bps-nav-icon"></i>
            <span>Suppliers</span>
        </a>
    </div>

    <div class="bps-nav-section">
        <span class="bps-nav-section-label">Analytics</span>
        <a asp-area="Admin" asp-controller="Reports" asp-action="Index" class="@NavClass("Admin", "Reports")">
            <i class="bi bi-graph-up-arrow bps-nav-icon"></i>
            <span>Reports</span>
        </a>
    </div>
}

@* ── STAFF ONLY ───────────────────────────────────────────────────────────── *@
@if (User.IsInRole("Staff"))
{
    <div class="bps-nav-section">
        <span class="bps-nav-section-label">Overview</span>
        <a asp-area="Staff" asp-controller="Dashboard" asp-action="Index" class="@NavClass("Staff", "Dashboard")">
            <i class="bi bi-grid-1x2-fill bps-nav-icon"></i>
            <span>Dashboard</span>
        </a>
    </div>

    <div class="bps-nav-section">
        <span class="bps-nav-section-label">Pharmacy Operations</span>
        <a asp-area="Staff" asp-controller="Patients" asp-action="Index" class="@NavClass("Staff", "Patients")">
            <i class="bi bi-people-fill bps-nav-icon"></i>
            <span>Patients</span>
        </a>
        <a asp-area="Staff" asp-controller="Medicines" asp-action="Index" class="@NavClass("Staff", "Medicines")">
            <i class="bi bi-capsule bps-nav-icon"></i>
            <span>Medicines Stock</span>
        </a>
        <a asp-area="Staff" asp-controller="Prescriptions" asp-action="Index" class="@NavClass("Staff", "Prescriptions")">
            <i class="bi bi-file-earmark-medical-fill bps-nav-icon"></i>
            <span>Prescriptions</span>
        </a>
        <a asp-area="Staff" asp-controller="Dispensing" asp-action="Index" class="@NavClass("Staff", "Dispensing")">
            <i class="bi bi-bag-heart-fill bps-nav-icon"></i>
            <span>Dispensing</span>
        </a>
        <a asp-area="Staff" asp-controller="RefillRequests" asp-action="Index" class="@NavClass("Staff", "RefillRequests")">
            <i class="bi bi-arrow-repeat bps-nav-icon"></i>
            <span>Refill Requests</span>
        </a>
    </div>
}

@* ── PATIENT ONLY ─────────────────────────────────────────────────────────── *@
@if (User.IsInRole("Patient"))
{
    <div class="bps-nav-section">
        <span class="bps-nav-section-label">Overview</span>
        <a asp-area="Patient" asp-controller="Dashboard" asp-action="Index" class="@NavClass("Patient", "Dashboard")">
            <i class="bi bi-grid-1x2-fill bps-nav-icon"></i>
            <span>Dashboard</span>
        </a>
    </div>

    <div class="bps-nav-section">
        <span class="bps-nav-section-label">My Health</span>
        <a asp-area="Patient" asp-controller="Profile" asp-action="Index" class="@NavClass("Patient", "Profile")">
            <i class="bi bi-person-circle bps-nav-icon"></i>
            <span>My Profile</span>
        </a>
        <a asp-area="Patient" asp-controller="Prescriptions" asp-action="Index" class="@NavClass("Patient", "Prescriptions")">
            <i class="bi bi-file-earmark-medical-fill bps-nav-icon"></i>
            <span>My Prescriptions</span>
        </a>
        <a asp-area="Patient" asp-controller="History" asp-action="Index" class="@NavClass("Patient", "History")">
            <i class="bi bi-clock-history bps-nav-icon"></i>
            <span>Dispensing History</span>
        </a>
        <a asp-area="Patient" asp-controller="Refills" asp-action="Index" class="@NavClass("Patient", "Refills")">
            <i class="bi bi-arrow-repeat bps-nav-icon"></i>
            <span>Refill Requests</span>
        </a>
    </div>
}
